version: '3.8'

services:
  web-app:
    build: .
    container_name: express-compose-app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
